// Shared focus-visible ring for keyboard navigation, used across all
// interactive elements (links, buttons) so focus styling stays consistent.
export const FOCUS_RING =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-text/40 focus-visible:ring-offset-2 focus-visible:ring-offset-bg'
